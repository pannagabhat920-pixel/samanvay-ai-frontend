import { ArrowUpRight, CircleDot, Gauge, Map, Radio, Route, ShieldCheck, Signal, Wrench, Zap } from "lucide-react";
import { Link } from "react-router-dom";
import { Badge } from "../components/ui/badge";
import { Button } from "../components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "../components/ui/card";
import { Separator } from "../components/ui/separator";
import { LiveCorridor, StatusLegend } from "../components/dashboard/OperationsWidgets";
import { CorridorRail } from "../components/corridor/CorridorRail";
import { demoDashboard, demoDepartments } from "../data/demo";

export function CorridorPage() {
  return <div className="space-y-5 pb-10 animate-fade-up"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2 text-xs text-text-muted"><Link to="/operations" className="hover:text-text-primary">Overview</Link><span className="text-border-strong">/</span><span>Corridor</span></div><h1 className="mt-3 text-2xl font-semibold tracking-[-0.035em] text-text-primary sm:text-3xl">Corridor control view</h1><p className="mt-2 text-sm text-text-secondary">A shared view of work windows, departments, and block pressure.</p></div><div className="flex items-center gap-2"><Badge variant="success"><span className="h-1.5 w-1.5 animate-soft-pulse rounded-full bg-status-safe" /> API foundation ready</Badge><Button asChild variant="outline" size="sm"><Link to="/new-request">Add request <ArrowUpRight className="h-3.5 w-3.5" /></Link></Button></div></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{demoDashboard.corridorDetails.map((item, index) => { const Icon = [Route, Gauge, Radio, ShieldCheck][index] || Route; return <Card key={item.label}><CardContent className="p-4"><div className="flex items-center justify-between"><Icon className="h-4 w-4 text-text-muted" /><span className="font-mono text-[9px] uppercase tracking-wider text-text-muted">Live</span></div><div className="metric-number mt-5 font-mono text-xl text-text-primary">{item.value}</div><div className="mt-1 text-xs text-text-secondary">{item.label}</div></CardContent></Card>; })}</div><CorridorRail /><LiveCorridor /><div className="grid gap-4 lg:grid-cols-[1.15fr_0.85fr]"><Card><CardHeader><div className="flex items-center gap-2"><Map className="h-4 w-4 text-accent" /><CardTitle>Department presence</CardTitle></div></CardHeader><CardContent className="space-y-4">{demoDepartments.map((department, index) => { const icons = [Wrench, Signal, Zap]; const Icon = icons[index]; const colors = ["text-dept-engineering", "text-dept-st", "text-dept-trd"]; return <div key={department.code} className="flex items-center gap-3"><div className={`flex h-8 w-8 items-center justify-center rounded-lg border border-border bg-surface2 ${colors[index]}`}><Icon className="h-4 w-4" /></div><div className="min-w-0 flex-1"><div className="text-xs font-medium text-text-primary">{department.label}</div><div className="mt-1 text-[10px] text-text-muted">{department.focus}</div></div><Badge variant="secondary">{String(department.requestCount ?? 0).padStart(2, "0")} requests</Badge></div>; })}</CardContent></Card><Card><CardHeader><div className="flex items-center gap-2"><CircleDot className="h-4 w-4 text-status-warning" /><CardTitle>Operating note</CardTitle></div></CardHeader><CardContent><p className="text-sm leading-6 text-text-secondary">Corridor recommendations remain advisory until an officer reviews the linked requests and confirms the block window.</p><Separator className="my-5" /><div className="space-y-3 text-xs text-text-muted"><div className="flex justify-between"><span>Last corridor sync</span><span className="font-mono text-text-secondary">{demoDashboard.corridor.lastSync}</span></div><div className="flex justify-between"><span>Data mode</span><span className="font-mono text-status-safe">foundation preview</span></div></div></CardContent></Card></div><StatusLegend /></div>;
}
